let initialState = {
    sideFriendBlock:
        [
            {id: 1, name: 'Alex'},
            {id: 2, name: 'Lina'},
            {id: 3, name: 'Fatima'}
        ]
}

const sidebarReducer = (state = initialState) => {
    return state;
}

export default sidebarReducer;